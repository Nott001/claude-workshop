# Accepted findings, by fingerprint (commit:path:rule:line).
#
# Each entry needs a reason. A blanket ignore, or a rule disabled globally, ends
# with the scan being treated as noise — which is the failure mode that lets a
# real key through.
#
# 65401bd — an invented invite token in three test fixtures. The value never
# existed anywhere: it is a placeholder for a Supabase hashed_token, and it
# matched `generic-api-key` only because 24 mixed alphanumerics after `token=`
# clear the entropy threshold. The fixtures were rewritten to be visibly
# placeholders, but that commit is already in this branch's history and gitleaks
# scans history, so the finding needs retiring by fingerprint as well.
65401bd71b9fcceda5608e8e36d5cba3037127c5:test/api-organization-invite.test.ts:generic-api-key:37
65401bd71b9fcceda5608e8e36d5cba3037127c5:test/invite-redirect.test.ts:generic-api-key:21
65401bd71b9fcceda5608e8e36d5cba3037127c5:test/invite-redirect.test.ts:generic-api-key:33
